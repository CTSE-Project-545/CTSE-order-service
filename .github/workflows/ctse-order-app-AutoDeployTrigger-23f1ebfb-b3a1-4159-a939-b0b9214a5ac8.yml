name: Trigger auto deployment for ctse-order-app

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ctse-order-app-AutoDeployTrigger-23f1ebfb-b3a1-4159-a939-b0b9214a5ac8.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CTSEORDERAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CTSEORDERAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CTSEORDERAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: ctseauthcontainer.azurecr.io
          registryUsername: ${{ secrets.CTSEORDERAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CTSEORDERAPP_REGISTRY_PASSWORD }}
          containerAppName: ctse-order-app
          resourceGroup: CTSE_GROUP
          imageToBuild: ctseauthcontainer.azurecr.io/ctse-order-app:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


